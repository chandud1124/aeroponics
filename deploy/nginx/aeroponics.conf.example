server {
    listen 80;
    server_name chandugowda.site www.chandugowda.site;

    location = /hydroponics {
        return 301 /hydroponics/;
    }

    location /hydroponics/ {
        proxy_pass http://127.0.0.1:8080/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Certbot will replace this with the HTTPS server block and redirect HTTP to HTTPS.
# After running `sudo certbot --nginx`, keep the upstream pointing at 127.0.0.1:8080.
